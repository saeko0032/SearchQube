<Page x:Class="SearchQube.Views.ErrorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:SearchQube.ViewModels"
        mc:Ignorable="d"
        Title="Error" Height="350" Width="525">
    <Grid>
        <Grid.DataContext>
            <vm:ErrorViewModel />
        </Grid.DataContext>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Error Occured! Back to Home." FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,100,0,0" />
        <Button Grid.Row="1" Content="Back" HorizontalAlignment="Center" VerticalAlignment="Center" Width="100" Height="30" Click="BackButton_Click" />
    </Grid>
</Page>
